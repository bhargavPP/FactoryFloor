# FactoryFloor.BuildingBlocks

Purpose
- Shared building blocks and helper utilities for services. Currently a placeholder project.

Notes
- Add common utilities here that are safe to share across services (e.g., custom middleware, shared exceptions, logging helpers). 
