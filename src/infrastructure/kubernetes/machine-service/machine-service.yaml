apiVersion: v1
kind: Service
metadata:
  name: machine-service
  namespace: default
spec:
  selector:
    app: machine-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP